{
  "name": "air-grab-lab",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/pack-moon-model.mjs && tsc --noEmit && vite build && vite build --config vite.moon.config.ts && node scripts/finish-moon.mjs && vite build --config vite.factory.config.ts",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "0.10.32",
    "three": "0.185.0",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "lucide-react": "1.31.0"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.0",
    "vitest": "^3.2.4"
  }
}
